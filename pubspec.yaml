name: address_formatter
description: Converts address components into correctly formatted postal addresses for countries worldwide, following the OpenCage address-formatting specification.
version: 0.2.0
repository: https://github.com/pm-gwatch/address_formatter

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  mustache_template: ^2.0.5

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
  http: ^1.6.0
  yaml: ^3.1.3
