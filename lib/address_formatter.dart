/// Address formatting following the OpenCage address-formatting specification.
///
/// The main entry point is [AddressFormatter], which exposes three static
/// methods: [AddressFormatter.format], [AddressFormatter.multiLineFormat],
/// and [AddressFormatter.singleLineFormat].
library;

export 'src/address_formatter.dart';
